{
  "name": "directory",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "npm run predeploy && ghpages -p .ghpages && shx rm -rf .ghpages",
    "predeploy": "shx mkdir -p .ghpages && shx cp -r index.html css .ghpages",
    "start": "npm run watch",
    "watch": "node scripts/watch.js"
  },
  "dependencies": {
    "ghpages": "0.0.10",
    "nunjucks": "^3.1.2",
    "shx": "^0.2.2"
  }
}
